name: Maven Build

on: [push]

jobs:
  build:
    name: Java 8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - run: java -version
      - run: mvn --version
      - run: mvn -B package
